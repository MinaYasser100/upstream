import 'package:flutter/material.dart';
import 'package:upstream/features/book/ui/widgets/book_study_body_view.dart';

class BookStudyView extends StatelessWidget {
  const BookStudyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: BookStudyBodyView());
  }
}
